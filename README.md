# Python Flask App for Jenkins CI/CD

This is a simple Python Flask app that returns a message with **version** and **environment**.

## Environment Variables

- APP_VERSION: App version (default v1.0)
- ENVIRONMENT: Environment (default dev)

## Run Locally


     pip install -r requirements.txt
     python app.py

##  NOTE
*This like add user to the Docker group
*Opening the inbound port 5000
*agent label [ can be different depending on what you configured"
*branch name 
*Docker image name should be in lower case 
*You should install the pip and virtual environment manually in the agent in this project 
*The credential name might be different so 

## so please check and use all those things
